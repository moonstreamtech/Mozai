# Node / build outputs
node_modules/
dist/
.cache/
.parcel-cache/
*.tsbuildinfo

# IDE / editor metadata
.idea/
*.iml
.vscode/
.DS_Store
.history/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Env files — ad ids must come from CI secrets, never the repo
.env
.env.local
.env.*.local

# Capacitor / Android build outputs.
# The android/ folder itself is generated by `npx cap add android` and is
# committed (Capacitor convention so native code lives in source control),
# but its build outputs are not.
android/.gradle/
android/.idea/
android/app/build/
android/app/release/
android/build/
android/capacitor-cordova-android-plugins/
android/local.properties
android/*.iml
android/app/*.iml

# Keystores: never commit. Release keystore is materialised at CI build time
# from a base64 secret (RELEASE_KEYSTORE_BASE64) mirroring Last Tile's flow.
*.keystore
*.jks
release.keystore

# iOS — not in scope for v1 but exclude generated build artefacts pre-emptively
ios/App/build/
ios/App/Pods/
ios/App/App.xcworkspace/xcuserdata/
ios/DerivedData/
