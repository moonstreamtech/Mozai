<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <!--
            Use the layer-list splash_centered.xml instead of the raw
            @drawable/splash bitmap. The bitmap inside the layer-list
            sets android:gravity="center" so the launch logo keeps
            its native 1:1 aspect ratio instead of being stretched to
            fill the screen.
        -->
        <item name="android:background">@drawable/splash_centered</item>
    </style>
</resources>